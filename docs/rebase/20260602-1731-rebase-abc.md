# Rebase + merge run: 20260602-1731-rebase-abc

- **Date:** 2026-06-02 17:31
- **Base:** main @ 45b1d96 → advanced to 5cf7efb mid-run (user committed untracked WIP: skill + plans). Branches re-rebased onto 5cf7efb.
- **Targets:** test/rebase-a, test/rebase-b, test/rebase-c
- **Backups:** orch-backup/20260602-1731-rebase-abc/* (restore: `scripts/restore-backup.sh 20260602-1731-rebase-abc test/rebase-a test/rebase-b test/rebase-c`)

## Worktree summaries

### test/rebase-a
Rewrites line 2 of `rebase-test.txt` to "CHANGED BY BRANCH A". Single-file, single-line change. Risk: line 2 is explicitly shared contested content.

### test/rebase-b
Rewrites line 2 of `rebase-test.txt` to "CHANGED BY BRANCH B". Single-file, single-line change. Risk: same contested line as branch A — direct collision.

### test/rebase-c
Adds new file `rebase-c-feature.txt` with one line. Purely additive, no shared files. Risk: none.

## Conflict prediction

- vs main: test/rebase-a CLEAN, test/rebase-b CLEAN, test/rebase-c CLEAN
- shared files: test/rebase-a × test/rebase-b → rebase-test.txt (predicted textual conflict: YES)
- test/rebase-c independent of both

## Decided order & rationale

1. **test/rebase-c** — fully independent (new file only), lands first with zero risk.
2. **test/rebase-a** — first to touch line 2 of `rebase-test.txt`; rebases onto a main that only has c's new file, so CLEAN.
3. **test/rebase-b** — also rewrites line 2; after a lands, main's line 2 = "CHANGED BY BRANCH A". **Expect a rebase conflict** on `rebase-test.txt:2`.

### Interaction warnings
- **test/rebase-b after test/rebase-a:** both rewrite the SAME line (`rebase-test.txt` line 2). Once a lands, b's rebase will conflict on that line. This collision is inherent — both want to own line 2. Resolution requires a human decision on which content (or a merge of both) the final line should hold. Default: preserve both intents if sensible, else last-writer (b) wins per its commit intent.

## Run log

- [x] test/rebase-c — rebase: CLEAN  merge: MERGED (5cf7efb → f8b24c5)  verify: PASS
- [x] test/rebase-a — rebase: CLEAN  merge: MERGED (f1f9d56 → ef90e65)  verify: PASS (after clearing leaked tmux/puppet daemons — env flakiness, not integration)
- [x] test/rebase-b — rebase: CONFLICT on rebase-test.txt:2 (resolved: kept BOTH A's and B's lines)  merge: MERGED (ef90e65 → c47c424)  verify: PASS

## Result

main @ c47c424. Merged: test/rebase-c, test/rebase-a, test/rebase-b (in that order). Skipped: none.

### Conflicts & fixes
- **test/rebase-b** rebase conflicted on `rebase-test.txt` line 2 (A and B both rewrote it) — as predicted. Resolved by keeping both lines ("CHANGED BY BRANCH A" + "CHANGED BY BRANCH B"). No source/integration fixes were needed; the two `verify` FAILs seen mid-run were pre-existing real-tmux daemon-leak flakiness (`rebase-test.txt` is referenced nowhere in src/tests), cleared by killing leaked daemons.

### Restore
`scripts/restore-backup.sh 20260602-1731-rebase-abc test/rebase-a test/rebase-b test/rebase-c`
(Note: the main backup tag points at 45b1d96, the pre-run tip; main has since advanced through your WIP commits + these three merges.)
