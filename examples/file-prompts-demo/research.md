Research the feature idea slugged "{{slug}}" and write a short findings document to `{{sessionsDir}}/findings.md`.

Procedure:

1. Skim the working directory (READMEs, package.json, top-level docs) to understand the project shape.
2. Identify 3–6 concrete points relevant to the feature — prior art in the repo, related modules, constraints, or open questions.
3. Write `{{sessionsDir}}/findings.md` with one short paragraph per point. Use H2 headings.
4. Do not modify any source files; this is a read + write-findings pass only.

Write only the file. Do not print findings into your reply.
