// MOVED → tests-new/_support/type-assertions.ts (D13).
// This thin re-export shim keeps still-live old `tests/**` consumers resolving
// until they are skipped/relocated in a later migration phase, then is deleted.
export * from '../../tests-new/_support/type-assertions.ts'
