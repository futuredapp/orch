/orch-acceptance-tests {{sessionsDir}}/brainstorm.md

Read the brainstorm at `{{sessionsDir}}/brainstorm.md` and author plain-language behavioral
acceptance tests (Given/When/Then, one behavior per test, observable behavior only — never
implementation details) that together define "done" for this feature.

Write them to `{{sessionsDir}}/acceptance-tests.md`. Cover the change delta plus the few
behaviors that must not break, including negatives and edge cases.
