Read `{{sessionsDir}}/plan.md` and `{{sessionsDir}}/plan-review.md`.

Double-check each finding in the plan review against the acceptance contract
(`{{sessionsDir}}/brainstorm.md` and `{{sessionsDir}}/acceptance-tests.md`).

Apply the findings that genuinely improve the plan — but do NOT overcomplicate it and do NOT
expand scope beyond the brainstorm. Edit `{{sessionsDir}}/plan.md` in place, preserving the
`Status:` lines and the AI-vs-user-input separation on every phase.

Write which findings you applied and which you rejected — each with a one-line reason — to
`{{sessionsDir}}/plan-review-applied.md`.
