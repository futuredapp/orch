/review Perform a thorough code review of PRIMARILY the new changes introduced by this feature (the diff on the current branch since it diverged). Check correctness, bugs, edge cases, error handling, and security — and verify the changes fulfil the acceptance contract in `{{sessionsDir}}/brainstorm.md` and `{{sessionsDir}}/acceptance-tests.md`. Read those two files first for context. Do not flag — and never start changing — long-standing, pre-existing code unrelated to this change; if you spot an unrelated issue worth recording, write it as its own file under `{{sessionsDir}}/issues/` (clear, self-contained description a developer with no context can understand) instead of putting it in the review findings. Write every finding (for the new changes) to `{{sessionsDir}}/code-review-codex.md` with as much detail as possible: severity (critical / high / medium / low), `file:line`, a description, the rationale, and a concrete suggested fix.
