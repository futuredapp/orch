/compound-engineering:ce-brainstorm

The user reviewed the current spec and was NOT satisfied — this is an interactive refinement
session.

Start by ASKING the user what is wrong with the current brainstorm and acceptance tests and
what they want changed. Do not assume — let them tell you. Then, working with them, refine BOTH
the brainstorm and the acceptance tests per their answers and update
`{{sessionsDir}}/brainstorm.md` and `{{sessionsDir}}/acceptance-tests.md` in place so they stay
consistent with each other. Only change what the user is unhappy with; leave the rest alone.
